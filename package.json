{
  "name": "career-explorer",
  "version": "1.0.0",
  "private": true,
  "description": "Static TK–5 RIASEC career-interest exploration site",
  "scripts": {
    "test": "node --test tests/*.test.js && html-validate \"*.html\"",
    "test:logic": "node --test tests/*.test.js",
    "test:html": "html-validate \"*.html\""
  },
  "devDependencies": {
    "html-validate": "10.4.0"
  }
}
